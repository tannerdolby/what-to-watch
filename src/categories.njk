---
defaultTags:
    - Horror
    - Crime
    - Adventure
    - Action
    - Comedy
    - Drama
    - Mystery
    - Thriller
    - Science fiction
    - Spy
    - Adult animation
    - Animated sitcom
    - Sitcom
---
<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        {% metagen 
            title="Categories - What To Watch",
            desc="The categories page for What to Watch. A collection of TV shows and series on popular streaming services.",
            url="https://whatt2watch.netlify.app/",
            img="https://whatt2watch.netlify.app/images/what-to-watch.png",
            img_alt="A photo of the whattowatch website home page",
            twitterHandle="@tannerdolby",
            name="Tanner Dolby"
        %}
        <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x16.png">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <header class="main-nav">
            <nav>
                <a class="home-btn" href="/">Home</a>
                <ul class="nav-list">
                    <li><a href="/categories/">Categories</a></li>
                    <li><a href="/search/">Search</a></li>
                    <li><a href="https://github.com/tannerdolby/what-to-watch/blob/master/CONTRIBUTING.md">Contribute</a></li>
                </ul>
            </nav>
        </header>
        <div class="welcome-block">
            <h1 style="margin-top: 3rem;">{{ title }}</h1>
            <div class="welcome-intro">
                <h2>Need a new show?</h2>
                <p>You've come to the right place. Have a look at the collection of series below. Enjoy!</p>
            </div>
        </div>
        <main class="categories-main">
            <ul class="category-list">
            {% for genre in defaultTags %}
                <li><a href="#{{ genre | cleanup }}">{{ genre | cleanup }}</a></li>
            {% endfor %}
            </ul>
            <h2 id="drama">Drama</h2>
            <div class="shows-collection">
                {% for show in collections.Drama | reverse %}
                    <article class="show">
                        <h2>{{ show.data.title }}</h2>
                        <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                        <div class="series-data" style="display: flex; justify-content: space-between;">
                            <p>Seasons: {{ show.data.seasons }}</p>
                            <p>Episodes: {{ show.data.episodes }}</p>
                        </div>
                        <ul style="padding-left: 1.5rem;">
                            <li>Episode length: {{ show.data.runtime }}</li>
                            <li>Streaming on {{ show.data.streamingService }}</li>
                        </ul>
                        <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                        <h3>Genres</h3>
                        <ul class="genre">
                        {% for genre in show.data.tags %}
                             <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                        {% endfor %}
                        </ul>
                    </article>
                {% endfor %}
            </div>
            <h2 id="comedy">Comedy</h2>
            <div class="shows-collection">
                {% for show in collections.Comedy | reverse %}
                    <article class="show">
                        <h2>{{ show.data.title }}</h2>
                        <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                        <div class="series-data" style="display: flex; justify-content: space-between;">
                            <p>Seasons: {{ show.data.seasons }}</p>
                            <p>Episodes: {{ show.data.episodes }}</p>
                        </div>
                        <ul style="padding-left: 1.5rem;">
                            <li>Episode length: {{ show.data.runtime }}</li>
                            <li>Streaming on {{ show.data.streamingService }}</li>
                        </ul>
                        <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                        <h3>Genres</h3>
                        <ul class="genre">
                        {% for genre in show.data.tags %}
                             <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                        {% endfor %}
                        </ul>
                    </article>
                {% endfor %}
            </div>
            <h2 id="thriller">Thriller</h2>
            <div class="shows-collection">
            {% for show in collections.Thriller | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                         <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="mystery">Mystery</h2>
            <div class="shows-collection">
            {% for show in collections.Mystery | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                         <li><a href="/categories#{{ genre | slug }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="science-fiction">Science fiction</h2>
            <div class="shows-collection">
            {% for show in collections.Science_fiction | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                        <li><a href="/categories/#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="horror">Horror</h2>
            <div class="shows-collection">
            {% for show in collections.Horror | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                         <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="crime">Crime</h2>
            <div class="shows-collection">
                {% for show in collections.Crime | reverse %}
                    <article class="show">
                        <h2>{{ show.data.title }}</h2>
                        <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                        <div class="series-data" style="display: flex; justify-content: space-between;">
                            <p>Seasons: {{ show.data.seasons }}</p>
                            <p>Episodes: {{ show.data.episodes }}</p>
                        </div>
                        <ul style="padding-left: 1.5rem;">
                            <li>Episode length: {{ show.data.runtime }}</li>
                            <li>Streaming on {{ show.data.streamingService }}</li>
                        </ul>
                        <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                        <h3>Genres</h3>
                        <ul class="genre">
                        {% for genre in show.data.tags %}
                             <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                        {% endfor %}
                        </ul>
                    </article>
                {% endfor %}
            </div>
            <h2 id="action">Action</h2>
            <div class="shows-collection">
            {% for show in collections.Action | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                         <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner  }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="spy">Spy</h2>
            <div class="shows-collection">
                {% for show in collections.Spy | reverse %}
                    <article class="show">
                        <h2>{{ show.data.title }}</h2>
                        <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                        <div class="series-data" style="display: flex; justify-content: space-between;">
                            <p>Seasons: {{ show.data.seasons }}</p>
                            <p>Episodes: {{ show.data.episodes }}</p>
                        </div>
                        <ul style="padding-left: 1.5rem;">
                            <li>Episode length: {{ show.data.runtime }}</li>
                            <li>Streaming on {{ show.data.streamingService }}</li>
                        </ul>
                        <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                        <h3>Genres</h3>
                        <ul class="genre">
                        {% for genre in show.data.tags %}
                             <li><a href="/categories#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                        {% endfor %}
                        </ul>
                    </article>
                {% endfor %}
                </div>
            <h2 id="adventure">Adventure</h2>
            <div class="shows-collection">
            {% for show in collections.Adventure | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                        <li><a href="/categories/#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="sitcom">Sitcom</h2>
            <div class="shows-collection">
            {% for show in collections.Sitcom | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                        <li><a href="/categories/#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="adult-animation">Adult animation</h2>
            <div class="shows-collection">
            {% for show in collections.Adult_animation | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                        <li><a href="/categories/#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            <h2 id="animated-sitcom">Animated sitcom</h2>
            <div class="shows-collection">
            {% for show in collections.Animated_sitcom | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                        <li><a href="/categories/#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
            
            <h2 id="series">All Series</h2>
            <div class="shows-collection">
            {% for show in collections.shows | reverse %}
                <article class="show">
                    <h2>{{ show.data.title }}</h2>
                    <p style="margin-bottom: .5rem;"><em>Created {{ show.data.dateStr }}</em></p>
                    <div class="series-data" style="display: flex; justify-content: space-between;">
                        <p>Seasons: {{ show.data.seasons }}</p>
                        <p>Episodes: {{ show.data.episodes }}</p>
                    </div>
                    <ul style="padding-left: 1.5rem;">
                        <li>Episode length: {{ show.data.runtime }}</li>
                        <li>Streaming on {{ show.data.streamingService }}</li>
                    </ul>
                    <p><a class="link-to-wiki" href="{{ show.data.url }}">Read more &RightArrow;</a></p>
                    <h3>Genres</h3>
                    <ul class="genre">
                    {% for genre in show.data.tags %}
                        <li><a href="/categories/#{{ genre | cleaner }}">{{ genre | cleaner }}</a></li>
                    {% endfor %}
                    </ul>
                </article>
            {% endfor %}
            </div>
        </main>
        <footer>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="https://github.com/tannerdolby/what-to-watch#readme">About</a></li>
                    <li><a href="https://github.com/tannerdolby/what-to-watch/blob/master/CONTRIBUTING.md">Contribute</a></li>
                    <li><a href="https://github.com/tannerdolby/what-to-watch/blob/master/{{ page.inputPath }}/">Edit this page on Github</a></li>
                </ul>
            </nav>
            &copy; 2020 Tanner Dolby. Built with Eleventy and contributions! 
        </footer>
    </body>
</html>